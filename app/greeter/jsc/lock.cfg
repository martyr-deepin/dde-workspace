Class("Lock",
        Description("lock"),

        Function("get_username", CString("username", "name of logged in user"),
		),

        Function("get_icon", String("icon", "icon of logged in user"),
		),

        Function("draw_background", Null(),
            JSValueRef("canvas", "The canvas element"),
		),

        Function("try_unlock", Boolean("flag", "unlock flag"),
            CString("password", "password to unlock"),
		),

        Function("unlock_succeed", Null(),
		),

        Function("need_pwd", Boolean("needed", "whether needed to type password"),
		),

        Function("switch_user", Null(),
		),

        Function("get_date", String("date", "formatted date"),
		),

        Function("detect_capslock", Boolean("capslock", "whether capslock is on"),
		),

        Function("draw_camera", Null(),
                 JSValueRef("canvas", "the canvas element"),
                 Number("dest_widht", "the width of canvas"),
                 Number("dest_height", "the height of canvas")
                 ),

        Function("webview_ok", Null()),
        Function("use_face_recognition_login", Boolean()),
)
