Class("Dock",
        Description("the dock module of deepin webkit"),
        Function("set_tray_icon_position", Null(),
            Number("id", "the id of the tray icon"),
            Number("x", "the x position"),
            Number("y", "the y position")
            ),
        Function("get_tray_icon_list", JSCode("array of tray icon's id")), 

        Function("get_task_list", JSCode("array of current runing client")),
        Function("get_active_window", Number("ID", "The ID of the activew window's xid")),
        Function("active_window", Null(), 
            Number("ID", "The ID of the window which will be actived")),
        Function("show_desktop", Null()),

        Signal("task_add", "{}"),
        Signal("task_remove", "{}"),
        Signal("active_change", "{}"),
        Signal("task_add", "{}"),
        Signal("show_desktop", "{}"),
     )
