Class("Desktop",
        Description("the desktop module of deepin webkit"),
        Function("notify_workarea_size", Null()),
        Function("get_desktop_items",
            JSCode("items", "the desktop items in json format")
            ),
        Function("move_to_desktop",
            String("path", "the actual path moved to"),
            String("path", "the path will be moved")
            ),
        Function("get_items_by_dir",
            JSCode("items", "the items in json format"),
            String("path", "the dir path")
            ),
        Function("monitor_dir", Null(),
            String("path", "the dir path which will be monitored, when it changed signal dir_changed will emit")
            ),
        Function("cancel_monitor_dir", Null(),
            String("path", "cancel the monitor")
            ),
        Function("item_rename", Null(),
            String("path", "the file to be renamed"),
            String("path", "new file name")
            ),
        Function("item_delete", Null(),
            AString("path", "the file(s) array to be removed")
            )
)
